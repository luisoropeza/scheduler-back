server:
  port: 8080

spring:
  application:
    name: gateway-service

gateway:
  provider-service-url: ${PROVIDER_SERVICE_URL:http://localhost:8081}
  schedule-service-url: ${SCHEDULE_SERVICE_URL:http://localhost:8082}
  appointment-service-url: ${APPOINTMENT_SERVICE_URL:http://localhost:8083}
